# KnowledgeHub AI 项目结构与技术架构

## 1. 项目简介

KnowledgeHub AI 是一个基于 Spring Boot + Redis + PostgreSQL/pgvector + RAG 的个人知识库智能问答系统。

**用户可以：**
- 注册/登录；
- 创建知识库；
- 上传 PDF / Markdown 文档；
- 系统异步解析文档；
- 对文档进行 chunk 切分；
- 使用 bge-m3 生成 embedding；
- 使用 pgvector 存储和检索向量；
- 用户基于知识库提问；
- 系统调用 DeepSeek API 生成回答；
- 返回答案和引用来源；
- Redis 负责登录态、验证码、缓存、锁、限流、排行榜、解析进度、幂等等真实场景。

## 2. 最终技术栈

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端 | Vue 3 + Vite + Element Plus + Axios | Vue 3.x | 页面展示 |
| 后端 | Java + Spring Boot | Java 17 + Spring Boot 3.3.x | 后端主体 |
| ORM | MyBatis-Plus | 3.5.x | CRUD、分页 |
| 数据库 | PostgreSQL | 16 | 业务数据存储 |
| 向量扩展 | pgvector | 0.7.x | embedding 向量检索 |
| 缓存/NoSQL | Redis | 7.x | 登录态、缓存、锁、限流等 |
| AI 框架 | Spring AI | 1.0.x（轻量使用） | 模型调用封装 |
| Embedding 模型 | bge-m3 | - | 文本向量化（1024 维） |
| LLM | DeepSeek API | deepseek-chat | 问答生成 |
| 文档解析 | PDFBox + flexmark-java | - | PDF/Markdown 文本提取 |
| 鉴权 | JWT（jjwt） + Redis + Interceptor | - | 登录认证 |
| 异步 | @Async + ThreadPoolTaskExecutor | - | 文档解析异步化 |
| 接口文档 | Knife4j | 4.x | API 在线调试 |
| 构建工具 | Maven | 3.8+ | 依赖管理 |
| 部署 | Docker Compose | - | 本地依赖服务（PostgreSQL、Redis） |

## 3. 总体架构图

```text
┌─────────────────────────────────────────────────┐
│                  Vue 3 前端                       │
│  Login │ KnowledgeBase │ DocumentManage │ Chat   │
│              │ Dashboard                         │
└────────────────────┬────────────────────────────┘
                     │ HTTP (JSON)
                     │ Authorization: Bearer {token}
                     ▼
┌─────────────────────────────────────────────────┐
│           Spring Boot 3 后端 (port 8080)          │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 用户模块  │ │ 知识库    │ │ 文档模块          │ │
│  │ user     │ │ kb       │ │ document         │ │
│  └──────────┘ └──────────┘ └──────────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ Redis    │ │ 问答模块  │ │ AI 能力模块       │ │
│  │ 工程能力  │ │ chat     │ │ ai               │ │
│  │          │ │          │ │ ┌──────────────┐  │ │
│  │ token    │ │ pipeline │ │ │Embedding     │  │ │
│  │ captcha  │ │ retrieve │ │ │Service(bge)  │  │ │
│  │ cache    │ │ prompt   │ │ │VectorRetriever│ │ │
│  │ lock     │ │ generate │ │ │(pgvector)    │  │ │
│  │ limit    │ │ sources  │ │ │LlmService    │  │ │
│  │ rank     │ │          │ │ │(DeepSeek)    │  │ │
│  │ progress │ │          │ │ └──────────────┘  │ │
│  │ idempot  │ │          │ │                   │ │
│  └──────────┘ └──────────┘ └──────────────────┘ │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │ 公共层：Result / ErrorCode / Exception /     │ │
│  │ Interceptor / AOP / UserContext / Config     │ │
│  └─────────────────────────────────────────────┘ │
└──────┬──────────────┬────────────────────────────┘
       │              │
       ▼              ▼
┌─────────────┐ ┌──────────┐
│ PostgreSQL  │ │  Redis   │
│ + pgvector  │ │  7.x     │
│ (5432)      │ │ (6379)   │
└─────────────┘ └──────────┘
       │
       ▼
┌─────────────┐ ┌──────────┐
│  bge-m3    │ │ DeepSeek │
│ Embedding  │ │   API    │
│  Service   │ │          │
└─────────────┘ └──────────┘
```

## 4. 后端目录结构

```text
knowledgehub-backend
├── pom.xml
├── docker-compose.yml
├── README.md
└── src
    ├── main
    │   ├── java/com/yourname/knowledgehub
    │   │   ├── KnowledgeHubApplication.java       ← 启动类
    │   │   │
    │   │   ├── common/                             ← 公共类
    │   │   │   ├── Result.java                     ← 统一返回体
    │   │   │   ├── ErrorCode.java                  ← 错误码枚举
    │   │   │   └── PageResult.java                 ← 分页返回体
    │   │   │
    │   │   ├── exception/                          ← 异常处理
    │   │   │   ├── BusinessException.java          ← 业务异常
    │   │   │   └── GlobalExceptionHandler.java     ← 全局异常处理
    │   │   │
    │   │   ├── config/                             ← 配置类
    │   │   │   ├── MyBatisPlusConfig.java          ← MyBatis-Plus 配置
    │   │   │   ├── RedisConfig.java                ← Redis 配置
    │   │   │   ├── Knife4jConfig.java              ← Knife4j 配置
    │   │   │   ├── AsyncConfig.java                ← 异步线程池配置
    │   │   │   ├── WebMvcConfig.java               ← MVC 配置（注册拦截器）
    │   │   │   └── CorsConfig.java                 ← 跨域配置
    │   │   │
    │   │   ├── interceptor/                        ← 拦截器
    │   │   │   └── LoginInterceptor.java           ← 登录鉴权拦截器
    │   │   │
    │   │   ├── aspect/                             ← AOP 切面
    │   │   │   ├── RequestLogAspect.java           ← 请求日志切面
    │   │   │   └── RateLimitAspect.java            ← 限流注解切面
    │   │   │
    │   │   ├── context/                            ← 上下文
    │   │   │   └── UserContext.java                ← ThreadLocal 用户上下文
    │   │   │
    │   │   ├── redis/                              ← Redis 工程能力
    │   │   │   ├── RedisKeyBuilder.java            ← Key 构建器
    │   │   │   ├── RedisKey.java                   ← Key 常量
    │   │   │   ├── RedisLockService.java           ← 分布式锁
    │   │   │   ├── RateLimitService.java           ← 限流服务
    │   │   │   ├── CacheService.java               ← 通用缓存服务
    │   │   │   ├── CaptchaService.java             ← 验证码服务
    │   │   │   ├── RankingService.java             ← ZSet 排行榜
    │   │   │   ├── IdempotentService.java          ← 幂等服务
    │   │   │   └── ParseProgressService.java       ← 解析进度服务
    │   │   │
    │   │   ├── module/                             ← 业务模块
    │   │   │   ├── user/                           ← 用户模块
    │   │   │   │   ├── entity/User.java
    │   │   │   │   ├── mapper/UserMapper.java
    │   │   │   │   ├── service/UserService.java
    │   │   │   │   ├── controller/UserController.java
    │   │   │   │   ├── dto/RegisterDTO.java
    │   │   │   │   ├── dto/LoginDTO.java
    │   │   │   │   └── vo/UserVO.java
    │   │   │   │
    │   │   │   ├── kb/                             ← 知识库模块
    │   │   │   │   ├── entity/KnowledgeBase.java
    │   │   │   │   ├── mapper/KbMapper.java
    │   │   │   │   ├── service/KbService.java
    │   │   │   │   ├── controller/KbController.java
    │   │   │   │   ├── dto/KbCreateDTO.java
    │   │   │   │   └── vo/KbVO.java
    │   │   │   │
    │   │   │   ├── document/                       ← 文档模块
    │   │   │   │   ├── entity/Document.java
    │   │   │   │   ├── mapper/DocumentMapper.java
    │   │   │   │   ├── service/DocumentService.java
    │   │   │   │   ├── controller/DocumentController.java
    │   │   │   │   ├── dto/DocumentUploadDTO.java
    │   │   │   │   └── vo/DocumentVO.java
    │   │   │   │
    │   │   │   ├── chunk/                          ← Chunk 模块
    │   │   │   │   ├── entity/DocumentChunk.java
    │   │   │   │   ├── mapper/ChunkMapper.java
    │   │   │   │   └── service/ChunkService.java
    │   │   │   │
    │   │   │   ├── chat/                           ← 问答模块
    │   │   │   │   ├── entity/ChatRecord.java
    │   │   │   │   ├── mapper/ChatRecordMapper.java
    │   │   │   │   ├── service/ChatService.java
    │   │   │   │   ├── controller/ChatController.java
    │   │   │   │   ├── dto/AskDTO.java
    │   │   │   │   └── vo/AnswerVO.java
    │   │   │   │
    │   │   │   └── ai/                             ← AI 能力模块
    │   │   │       ├── entity/ModelCallLog.java
    │   │   │       ├── mapper/ModelCallLogMapper.java
    │   │   │       ├── service/
    │   │   │       │   ├── EmbeddingService.java   ← bge-m3 调用
    │   │   │       │   ├── LlmService.java         ← DeepSeek 调用
    │   │   │       │   ├── DocumentParserService.java ← 文档解析
    │   │   │       │   ├── TextSplitter.java       ← chunk 切分
    │   │   │       │   ├── PgVectorRetriever.java  ← pgvector 检索
    │   │   │       │   ├── PromptBuilder.java      ← Prompt 构造
    │   │   │       │   ├── RagPipelineService.java ← RAG 主流程
    │   │   │       │   ├── RelevanceCheckService.java ← 拒答判断
    │   │   │       │   ├── SourceAssembler.java    ← 引用组装
    │   │   │       │   └── DocumentIngestService.java ← 文档入库编排
    │   │   │       └── dto/
    │   │   │
    │   │   └── utils/                              ← 工具类
    │   │       ├── JwtUtils.java                   ← JWT 工具
    │   │       └── PasswordUtils.java              ← 密码加密
    │   │
    │   └── resources/
    │       ├── application.yml                     ← 主配置
    │       ├── application-dev.yml                 ← 开发环境配置
    │       └── mapper/                             ← MyBatis XML（如需要）
    │
    └── test/java/com/yourname/knowledgehub/       ← 测试
```

### 目录职责说明

| 目录 | 职责 |
|------|------|
| `common/` | 统一返回体 Result、错误码 ErrorCode、分页返回 PageResult |
| `exception/` | 自定义业务异常 BusinessException、全局异常处理器（@RestControllerAdvice） |
| `config/` | Spring Boot 配置类：MyBatis-Plus、Redis、Knife4j、Async、CORS、MVC |
| `interceptor/` | 登录拦截器，从 Redis 校验 token，设置 UserContext |
| `aspect/` | AOP 切面：请求日志（环绕通知）、限流注解 |
| `context/` | ThreadLocal 用户上下文，存取当前请求的 userId |
| `redis/` | Redis 工程能力层，封装所有 Redis 操作：锁、限流、缓存、排行榜等 |
| `module/user/` | 用户注册、登录、个人信息 |
| `module/kb/` | 知识库 CRUD，用户隔离 |
| `module/document/` | 文档上传、元数据管理、解析触发、进度查询 |
| `module/chunk/` | document_chunk 表操作 |
| `module/chat/` | 问答记录、对话历史 |
| `module/ai/` | AI 能力层：Embedding、LLM、解析、切分、检索、RAG 编排 |
| `utils/` | 工具类：JWT 生成/解析、密码 BCrypt 加密 |

## 5. 前端目录结构

```text
knowledgehub-web
├── index.html
├── package.json
├── vite.config.js
├── src
│   ├── main.js                         ← 入口
│   ├── App.vue                         ← 根组件
│   │
│   ├── api/                            ← 接口封装（集中管理）
│   │   ├── request.js                  ← Axios 实例 + 拦截器
│   │   ├── user.js                     ← 用户相关 API
│   │   ├── kb.js                       ← 知识库相关 API
│   │   ├── document.js                 ← 文档相关 API
│   │   ├── chat.js                     ← 问答相关 API
│   │   └── dashboard.js               ← 看板相关 API
│   │
│   ├── router/                         ← 路由
│   │   └── index.js
│   │
│   ├── utils/                          ← 工具
│   │   └── request.js                  ← Axios 封装（统一 token、统一错误提示）
│   │
│   ├── views/                          ← 页面
│   │   ├── Login.vue                   ← 登录页
│   │   ├── KnowledgeBase.vue           ← 知识库管理页
│   │   ├── DocumentManage.vue          ← 文档管理页
│   │   ├── Chat.vue                    ← 问答页
│   │   └── Dashboard.vue              ← Redis 看板页
│   │
│   └── components/                     ← 通用组件
│       ├── NavBar.vue                  ← 顶部导航
│       └── SourceCard.vue             ← 引用来源卡片
```

### 前端说明
- **前端由 Claude Code 生成**，我负责验收；
- 所有 API 调用必须集中到 `src/api/` 目录；
- `request.js` 必须统一携带 token（从 localStorage 读取），统一处理 401 跳转；
- 每个 Vue 页面必须在代码顶部注释说明：页面职责、调用接口、请求参数、展示字段、对应 Redis 场景。

---

## 6. 数据库表设计

### sys_user（用户表）

```sql
CREATE TABLE sys_user (
    id          BIGSERIAL PRIMARY KEY,
    username    VARCHAR(50)  NOT NULL UNIQUE,
    password    VARCHAR(255) NOT NULL,
    email       VARCHAR(100),
    status      SMALLINT     DEFAULT 1,   -- 1=正常 0=禁用
    create_time TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    deleted     SMALLINT     DEFAULT 0    -- 逻辑删除 0=未删除 1=已删除
);
```

### knowledge_base（知识库表）

```sql
CREATE TABLE knowledge_base (
    id          BIGSERIAL PRIMARY KEY,
    user_id     BIGINT       NOT NULL,
    name        VARCHAR(100) NOT NULL,
    description TEXT,
    kb_version  INT          DEFAULT 1,   -- 知识库版本号，用于缓存失效
    create_time TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    deleted     SMALLINT     DEFAULT 0
);
CREATE INDEX idx_kb_user_id ON knowledge_base(user_id);
```

### document（文档表）

```sql
CREATE TABLE document (
    id          BIGSERIAL PRIMARY KEY,
    kb_id       BIGINT       NOT NULL,
    user_id     BIGINT       NOT NULL,
    title       VARCHAR(200) NOT NULL,
    file_type   VARCHAR(20),              -- PDF / MARKDOWN
    file_path   VARCHAR(500),             -- 本地存储路径
    parse_status SMALLINT    DEFAULT 0,   -- 0=待解析 1=解析中 2=解析成功 3=解析失败
    chunk_count INT          DEFAULT 0,   -- chunk 数量
    create_time TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    deleted     SMALLINT     DEFAULT 0
);
CREATE INDEX idx_document_kb_id ON document(kb_id);
CREATE INDEX idx_document_user_id ON document(user_id);
```

**parse_status 说明：**

| 值 | 状态 | 说明 |
|----|------|------|
| 0 | 待解析 | 文档上传后初始状态 |
| 1 | 解析中 | 异步任务已开始处理 |
| 2 | 解析成功 | chunk + embedding 入库完成 |
| 3 | 解析失败 | 解析或入库过程出错 |

### document_chunk（文档块表）

```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE document_chunk (
    id            BIGSERIAL PRIMARY KEY,
    document_id   BIGINT       NOT NULL,
    kb_id         BIGINT       NOT NULL,
    user_id       BIGINT       NOT NULL,
    chunk_index   INT          NOT NULL,   -- chunk 序号（从 0 开始）
    content       TEXT         NOT NULL,   -- chunk 文本内容
    token_count   INT,                     -- token 估算数
    page_no       INT,                     -- PDF 页码 / MD 章节编号
    section_title VARCHAR(200),            -- 所属章节标题
    embedding     vector(1024),            -- bge-m3 1024 维向量
    create_time   TIMESTAMP    DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_chunk_document_id ON document_chunk(document_id);
CREATE INDEX idx_chunk_kb_id ON document_chunk(kb_id);
-- pgvector 向量索引（后续 Day 22 手敲创建）
-- CREATE INDEX idx_chunk_embedding ON document_chunk USING ivfflat (embedding vector_cosine_ops);
```

### chat_record（问答记录表）

```sql
CREATE TABLE chat_record (
    id                   BIGSERIAL PRIMARY KEY,
    user_id              BIGINT       NOT NULL,
    kb_id                BIGINT       NOT NULL,
    question             TEXT         NOT NULL,
    rewritten_question   TEXT,                 -- 改写后的问题（可选）
    answer               TEXT,
    retrieved_chunk_ids  TEXT,                 -- JSON 数组，召回的 chunk ID 列表
    model_name           VARCHAR(50),          -- 使用的模型名称
    latency_ms           INT,                  -- 回答延迟（毫秒）
    create_time          TIMESTAMP    DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_chat_user_id ON chat_record(user_id);
CREATE INDEX idx_chat_kb_id ON chat_record(kb_id);
```

### model_call_log（模型调用日志表）

```sql
CREATE TABLE model_call_log (
    id               BIGSERIAL PRIMARY KEY,
    user_id          BIGINT,
    model_name       VARCHAR(50)  NOT NULL,
    call_type        VARCHAR(20)  NOT NULL,  -- EMBEDDING / LLM_CHAT
    prompt_tokens    INT,
    completion_tokens INT,
    latency_ms       INT,
    success          BOOLEAN      DEFAULT TRUE,
    error_message    TEXT,
    create_time      TIMESTAMP    DEFAULT CURRENT_TIMESTAMP
);
```

---

## 7. Redis 场景设计（12 个核心场景）

### 场景一览

| # | 场景 | Key | 类型 | TTL | 触发接口 |
|---|------|-----|------|-----|----------|
| 1 | 登录 token | `login:token:{token}` | String | 7 days | POST /api/user/login |
| 2 | 验证码 | `captcha:email:{email}` | String | 5 min | POST /api/user/captcha |
| 3 | 用户缓存 | `user:info:{userId}` | String | 30 min | GET /api/user/me |
| 4 | 知识库列表缓存 | `kb:list:{userId}` | String | 10-30 min 随机 | GET /api/kb/list |
| 5 | 文档详情缓存 | `doc:detail:{docId}` | String | 30-60 min 随机 | GET /api/document/{id} |
| 6 | 文档解析锁 | `lock:doc:parse:{docId}` | String | 10 min | POST /api/document/{id}/parse |
| 7 | 问答限流 | `rate:chat:{userId}:{minute}` | String | 60 sec | POST /api/chat/ask |
| 8 | 热门文档 | `rank:doc:hot` | ZSet | 无（按周期清理） | GET /api/document/{id} |
| 9 | 最近访问 | `recent:kb:{userId}` | ZSet | 30 days | GET /api/kb/{id} |
| 10 | 问答缓存 | `chat:answer:{kbId}:{kbVersion}:{questionHash}` | String | 30 min | POST /api/chat/ask |
| 11 | 解析进度 | `task:doc:parse:{docId}` | Hash | 24 h | GET /api/document/{id}/parse-status |
| 12 | 每日额度 | `quota:chat:daily:{userId}:{yyyyMMdd}` | String | 到当天 23:59:59 | POST /api/chat/ask |
| 13 | 幂等 token | `idem:token:{token}` | String | 5 min | 上传/创建/解析 |

### 每个场景详细设计

#### 场景 1：登录 token
- **为什么用 Redis：** 支持服务端主动失效（踢人、修改密码后强制下线），JWT 单独无法做到
- **Key 设计：** `login:token:{token}`，value = userId 的 JSON 或直接存 userId
- **TTL：** 7 天，每次请求可续期
- **面试追问：** JWT + Redis 是双写吗？token 要不要存用户信息？token 泄露怎么办？
- **后续优化：** 引入 Refresh Token 双 token 机制

#### 场景 2：验证码
- **为什么用 Redis：** 验证码是临时数据，天然适合 Redis 的 TTL
- **Key 设计：** `captcha:email:{email}`，value = 验证码
- **TTL：** 5 分钟
- **防重复发送：** 额外的 key `captcha:cooldown:{email}`，TTL 60s
- **面试追问：** 验证码校验后要不要删除？为什么要加冷却期？
- **后续优化：** 接入真实邮件服务 / 短信服务

#### 场景 3：用户信息缓存（Cache Aside）
- **为什么用 Redis：** 用户信息读多写少，缓存可大幅降低 DB 压力
- **Key 设计：** `user:info:{userId}`，value = 用户信息 JSON
- **TTL：** 30 分钟
- **面试追问：** Cache Aside 的读写顺序？为什么不是先更新 DB 再删缓存？
- **后续优化：** 使用 Canal 监听 binlog 自动失效缓存

#### 场景 4：知识库列表缓存
- **为什么用 Redis：** 列表查询频繁，缓存可加速页面加载
- **Key 设计：** `kb:list:{userId}`，value = 知识库列表 JSON
- **TTL：** 10-30 分钟随机（防雪崩）
- **面试追问：** 列表缓存和详情缓存的区别？分页列表怎么缓存？
- **后续优化：** 知识库更新时主动删除（已完成）

#### 场景 5：文档详情缓存 + 空值缓存
- **为什么用 Redis：** 文档详情查询频繁；空值缓存防穿透
- **Key 设计：** `doc:detail:{docId}`，value = 文档详情 JSON
- **TTL：** 正常 30-60 分钟随机；空值 5 分钟
- **面试追问：** 空值缓存和新数据写入的竞态问题？TTL 随机化的原理？
- **后续优化：** 布隆过滤器替代空值缓存

#### 场景 6：分布式锁
- **为什么用 Redis：** 防止同一文档被重复解析
- **Key 设计：** `lock:doc:parse:{docId}`，value = UUID
- **TTL：** 10 分钟（需评估解析任务最长耗时）
- **面试追问：** 为什么 value 用 UUID？为什么释放要用 Lua？锁过期了任务还没完成怎么办？
- **后续优化：** Redisson 看门狗自动续期

#### 场景 7：问答限流
- **为什么用 Redis：** INCR 天然适合计数；Lua 保证原子性
- **Key 设计：** `rate:chat:{userId}:{yyyyMMddHHmm}`（精确到分钟）
- **TTL：** 60 秒（固定窗口）
- **面试追问：** 固定窗口的临界问题？滑动窗口怎么实现？
- **后续优化：** 滑动窗口 / 令牌桶算法

#### 场景 8：热门文档排行榜（ZSet）
- **为什么用 Redis：** ZSet 的有序性天然适合排行榜
- **Key 设计：** `rank:doc:hot`，member = docId，score = 访问次数
- **TTL：** 无过期，可定期清理（如每周重置）
- **面试追问：** ZSet 底层结构？亿级数据量怎么存？
- **后续优化：** 分时间段排行（周榜、月榜）

#### 场景 9：最近访问知识库（ZSet）
- **为什么用 Redis：** ZSet 按时间戳排序，天然适合"最近"语义
- **Key 设计：** `recent:kb:{userId}`，member = kbId，score = 访问时间戳
- **TTL：** 30 天，超过后 ZREMRANGEBYRANK 清理
- **面试追问：** 时间戳做 score 有什么问题？怎么限制列表长度？
- **后续优化：** 结合前端展示最近 10 个

#### 场景 10：问答结果缓存
- **为什么用 Redis：** 相同问题命中缓存可节省 DeepSeek API 调用成本
- **Key 设计：** `chat:answer:{kbId}:{kbVersion}:{questionHash}`
- **TTL：** 30 分钟
- **面试追问：** 为什么 key 要包含 kbVersion？questionHash 怎么算？
- **后续优化：** 语义相似度去重（不仅是哈希匹配）

#### 场景 11：解析进度（Hash）
- **为什么用 Redis：** Hash 适合存储对象状态；前端轮询查询
- **Key 设计：** `task:doc:parse:{docId}`
- **字段：** status、totalChunks、completedChunks、currentStep、errorMsg、startTime
- **TTL：** 24 小时
- **面试追问：** 为什么用 Hash 而不是 String（JSON）？
- **后续优化：** WebSocket 推送进度替代轮询

#### 场景 12：每日提问额度
- **为什么用 Redis：** 计数器场景，INCR 原子操作
- **Key 设计：** `quota:chat:daily:{userId}:{yyyyMMdd}`
- **TTL：** 到当天 23:59:59
- **面试追问：** 第一次 INCR 时怎么保证 EXPIRE 也执行？跨天 key 切换？
- **后续优化：** 不同用户不同额度（VIP 用户）

#### 场景 13：幂等 token
- **为什么用 Redis：** SET + DEL 原子操作
- **Key 设计：** `idem:token:{token}`，value = "1"
- **TTL：** 5 分钟
- **面试追问：** 幂等和防重的区别？Lua 脚本为什么需要？
- **后续优化：** 结合业务单号实现更强幂等

---

## 8. 核心业务流程

### 8.1 用户登录流程

```text
前端 Login.vue
  │
  ▼
POST /api/user/login
  │ { username, password }
  ▼
UserService.login()
  ├── 查 DB 校验用户名密码 (BCrypt)
  ├── 生成 JWT token (JwtUtils)
  ├── Redis 写入 login:token:{token} (userId, TTL 7天)
  ├── Redis ZADD recent:kb:{userId} (可选：更新最近活跃)
  └── 返回 { token, userInfo }
  ▼
前端 localStorage.setItem('token', token)
  │
  ▼
后续请求统一携带 Authorization: Bearer {token}
  │
  ▼
LoginInterceptor.preHandle()
  ├── 从 Header 取 token
  ├── Redis 查 login:token:{token}
  ├── 存在 → 设置 UserContext.setUserId()
  ├── 不存在 → 返回 401
  └── 放行
```

### 8.2 文档入库流程

```text
前端 DocumentManage.vue
  │
  ▼
POST /api/document/upload
  │ { kbId, file }
  ▼
DocumentService.upload()
  ├── 保存文件到本地磁盘
  ├── 写入 document 表 (parse_status = 0)
  └── 返回 documentId
  ▼
POST /api/document/{id}/parse (手动触发)
  │
  ▼
DocumentService.triggerParse()
  ├── RedisLockService.tryLock("lock:doc:parse:{docId}")  ← 分布式锁
  ├── 更新 document.parse_status = 1 (解析中)
  ├── Redis Hash 初始化 task:doc:parse:{docId}
  └── 调用 @Async DocumentIngestService.ingest(docId)
  │
  ▼ (异步线程)
DocumentIngestService.ingest()
  ├── 更新 Hash: currentStep = "parsing"
  ├── DocumentParserService.parse(filePath) → 纯文本
  ├── 更新 Hash: currentStep = "splitting"
  ├── TextSplitter.split(text) → List<Chunk>
  ├── 更新 Hash: totalChunks = N, currentStep = "embedding"
  ├── FOR each chunk:
  │     EmbeddingService.embed(chunk.content) → vector(1024)
  │     更新 Hash: completedChunks++
  ├── 更新 Hash: currentStep = "saving"
  ├── ChunkService.batchInsert(chunks with embeddings)
  ├── 更新 document.parse_status = 2, chunk_count = N
  ├── 删除 Hash key (或标记完成)
  └── RedisLockService.unlock("lock:doc:parse:{docId}")
  │
  ▼
前端轮询 GET /api/document/{id}/parse-status
  → 返回 Redis Hash 中的进度信息
```

### 8.3 RAG 问答流程

```text
前端 Chat.vue
  │
  ▼
POST /api/chat/ask
  │ { kbId, question }
  ▼
RagPipelineService.ask()
  │
  ├── 1. RateLimitService.allow()        ← Redis 限流检查
  │     └── 不通过 → 返回 "请求过于频繁"
  │
  ├── 2. QuotaService.check()            ← 每日额度检查
  │     └── 不通过 → 返回 "今日提问次数已用完"
  │
  ├── 3. 权限校验：userId 是否有该 kbId 的访问权限
  │
  ├── 4. 问答缓存检查                      ← Redis 缓存
  │     key = chat:answer:{kbId}:{kbVersion}:{hash(question)}
  │     └── 命中 → 直接返回
  │
  ├── 5. EmbeddingService.embed(question)  ← bge-m3
  │     └── queryVector = float[1024]
  │
  ├── 6. PgVectorRetriever.retrieve(queryVector, kbId, topK=5)
  │     ← pgvector cosine distance <-> 检索
  │     └── List<ChunkWithScore>
  │
  ├── 7. RelevanceCheckService.check(topChunks)
  │     ├── 无结果 → 拒答 "知识库中暂无相关内容"
  │     └── 最高分 < 阈值 → 拒答 "未找到高度相关内容"
  │
  ├── 8. PromptBuilder.build(question, topChunks)
  │     ├── 系统提示词（角色设定 + 回答要求）
  │     ├── 上下文（chunk 内容拼接）
  │     ├── 用户问题
  │     └── 引用格式要求
  │
  ├── 9. LlmService.chat(prompt)          ← DeepSeek API
  │     ├── 记录 model_call_log
  │     └── answer
  │
  ├── 10. SourceAssembler.assemble(topChunks)
  │      └── List<Source> (documentTitle, pageNo, sectionTitle, content, score)
  │
  ├── 11. 保存 chat_record
  │
  ├── 12. 写入问答缓存
  │      key = chat:answer:{kbId}:{kbVersion}:{hash(question)}
  │
  └── 13. 返回 AnswerVO { answer, sources }
```

---

## 9. 后端接口设计

### 9.1 用户模块

| 接口 | 方法 | 说明 | 请求参数 | 返回数据 | 涉及 Redis |
|------|------|------|----------|----------|------------|
| /api/user/register | POST | 注册 | `{username, password, email}` | `{userId}` | - |
| /api/user/login | POST | 登录 | `{username, password}` | `{token, userInfo}` | login:token |
| /api/user/logout | POST | 退出 | Header: token | `{}` | 删除 login:token |
| /api/user/me | GET | 当前用户信息 | Header: token | `{id, username, email, status}` | 查 user:info 缓存 |
| /api/user/captcha | POST | 获取验证码 | `{email}` | `{captchaId}` | captcha:email + cooldown |

### 9.2 知识库模块

| 接口 | 方法 | 说明 | 请求参数 | 返回数据 | 涉及 Redis |
|------|------|------|----------|----------|------------|
| /api/kb/create | POST | 创建知识库 | `{name, description}` | `{kbId}` | 删 kb:list 缓存 |
| /api/kb/list | GET | 知识库列表 | `?page=1&size=10` | `{total, list: [{id,name,description,docCount}]}` | kb:list 缓存 |
| /api/kb/{id} | GET | 知识库详情 | Path: id | `{id, name, description, kbVersion, ...}` | ZADD recent:kb |
| /api/kb/{id} | PUT | 更新知识库 | `{name, description}` | `{}` | 删缓存, kbVersion++ |
| /api/kb/{id} | DELETE | 删除知识库 | Path: id | `{}` | 删相关缓存 |

### 9.3 文档模块

| 接口 | 方法 | 说明 | 请求参数 | 返回数据 | 涉及 Redis |
|------|------|------|----------|----------|------------|
| /api/document/upload | POST | 上传文档 | FormData: kbId, file | `{documentId}` | 幂等 token |
| /api/document/list | GET | 文档列表 | `?kbId=&page=&size=` | `{total, list: [{id, title, fileType, parseStatus, chunkCount}]}` | - |
| /api/document/{id} | GET | 文档详情 | Path: id | `{id, title, fileType, parseStatus, chunkCount, ...}` | doc:detail 缓存 |
| /api/document/{id} | DELETE | 删除文档 | Path: id | `{}` | 删缓存和相关 chunk |
| /api/document/{id}/parse | POST | 触发解析 | Path: id | `{}` | lock:doc:parse |
| /api/document/{id}/parse-status | GET | 解析进度 | Path: id | `{status, totalChunks, completedChunks, currentStep}` | task:doc:parse Hash |

### 9.4 问答模块

| 接口 | 方法 | 说明 | 请求参数 | 返回数据 | 涉及 Redis |
|------|------|------|----------|----------|------------|
| /api/chat/ask | POST | 提问 | `{kbId, question}` | `{answer, sources: [{documentTitle, pageNo, sectionTitle, content, score}]}` | rate + quota + answer cache |
| /api/chat/history | GET | 对话历史 | `?kbId=&page=&size=` | `{total, list: [{question, answer, createTime}]}` | - |
| /api/chat/retrieve | POST | 仅检索（调试） | `{kbId, question}` | `{chunks: [{content, score, documentTitle}]}` | - |

### 9.5 Dashboard 模块

| 接口 | 方法 | 说明 | 返回数据 | 涉及 Redis |
|------|------|------|----------|------------|
| /api/dashboard/hot-documents | GET | 热门文档 Top 10 | `[{docId, title, score}]` | ZREVRANGE rank:doc:hot |
| /api/dashboard/recent-kbs | GET | 最近访问知识库 | `[{kbId, name, timestamp}]` | ZREVRANGE recent:kb |
| /api/dashboard/chat-quota | GET | 今日提问额度 | `{used, limit}` | GET quota:chat:daily |
| /api/dashboard/parse-tasks | GET | 最近解析任务 | `[{docId, title, status, progress}]` | HGETALL task:doc:parse |

---

## 10. 前端页面与接口数据流

### Login.vue
```
页面职责：
  - 用户登录表单
  - 登录成功保存 token，跳转首页

调用接口：
  - POST /api/user/login
  - POST /api/user/captcha（可选）

请求参数：{ username: string, password: string }
返回数据：{ token: string, userInfo: { id, username, email } }

涉及 Redis：
  - captcha:email:{email}（验证码）
  - login:token:{token}（登录态）

关键流程：
  登录成功 → localStorage.setItem('token', data.token)
  → router.push('/knowledge-base')
```

### KnowledgeBase.vue
```
页面职责：
  - 知识库列表展示
  - 创建新知识库
  - 删除知识库
  - 点击进入文档管理

调用接口：
  - GET /api/kb/list?page=1&size=10
  - POST /api/kb/create
  - DELETE /api/kb/{id}

请求参数（创建）：{ name: string, description: string }
返回数据（列表）：{ total: number, list: [{id, name, description, createTime}] }

涉及 Redis：
  - kb:list:{userId}（列表缓存）
  - recent:kb:{userId}（最近访问）

关键流程：
  获取列表 → 展示卡片 → 点击创建弹窗 → 提交 → 刷新列表
  → 点击某个知识库 → 跳转 /documents?kbId=xxx
```

### DocumentManage.vue
```
页面职责：
  - 文档列表展示
  - 上传 PDF/Markdown 文档
  - 触发文档解析
  - 展示解析进度（轮询）

调用接口：
  - GET /api/document/list?kbId=xxx
  - POST /api/document/upload (FormData: kbId, file)
  - POST /api/document/{docId}/parse
  - GET /api/document/{docId}/parse-status

请求参数（上传）：FormData { kbId: number, file: File }
返回数据（进度）：{ status: string, totalChunks: number, completedChunks: number, currentStep: string }

涉及 Redis：
  - lock:doc:parse:{docId}（分布式锁）
  - task:doc:parse:{docId}（Hash 解析进度）
  - doc:detail:{docId}（文档缓存）

关键流程：
  上传文件 → 列表中显示 → 点击"解析"→ 开始轮询进度
  → 进度条更新 → 解析完成显示 chunk_count
```

### Chat.vue
```
页面职责：
  - 用户向指定知识库提问
  - 展示 DeepSeek 生成的回答
  - 展示 RAG 召回的引用来源

调用接口：
  - POST /api/chat/ask

请求参数：{ kbId: number, question: string }
返回数据：
  {
    answer: string,
    sources: [{
      documentTitle: string,
      pageNo: number,
      sectionTitle: string,
      content: string,
      score: number
    }]
  }

涉及 Redis：
  - rate:chat:{userId}:{minute}（限流）
  - quota:chat:daily:{userId}:{yyyyMMdd}（每日额度）
  - chat:answer:{kbId}:{kbVersion}:{questionHash}（问答缓存）

关键流程：
  输入问题 → 点击发送 → 显示 loading → 返回答案 + 来源卡片
  → 底部分引用来源（文档名、页码、相似度分数）
```

### Dashboard.vue
```
页面职责：
  - Redis 各场景数据可视化看板
  - 展示热门文档、最近访问、今日额度、解析任务

调用接口：
  - GET /api/dashboard/hot-documents
  - GET /api/dashboard/recent-kbs
  - GET /api/dashboard/chat-quota
  - GET /api/dashboard/parse-tasks

返回数据（热门文档）：
  [{docId, title, score}]

返回数据（最近访问）：
  [{kbId, name, accessedAt}]

返回数据（今日额度）：
  {used: number, limit: number}

返回数据（解析任务）：
  [{docId, title, status, progressPercent}]

涉及 Redis：
  - rank:doc:hot (ZSet)
  - recent:kb:{userId} (ZSet)
  - quota:chat:daily:{userId}:{yyyyMMdd} (String)
  - task:doc:parse:{docId} (Hash)

关键流程：
  进入页面 → 并行请求四个接口 → 四个卡片展示数据
```

---

## 11. 需要留 TODO 的核心类清单

### Redis 工程能力（全部留 TODO）
| 类 | 核心方法 | 涉及 Redis 命令 |
|----|----------|----------------|
| RedisKeyBuilder | buildKey() | - |
| RedisLockService | tryLock(), unlock() | SET NX EX, Lua DEL |
| RateLimitService | allow() | INCR, EXPIRE, Lua |
| CacheService | get(), set(), delete() | GET, SET, DEL |
| CaptchaService | generate(), verify() | SETEX, GET, DEL |
| RankingService | addScore(), getTopN() | ZINCRBY, ZREVRANGE |
| IdempotentService | checkAndConsume() | SET, DEL, Lua |
| ParseProgressService | init(), update(), get() | HSET, HGETALL |

### Spring Boot 核心（全部留 TODO）
| 类 | 核心方法 | 涉及知识点 |
|----|----------|-----------|
| LoginInterceptor | preHandle() | Interceptor, Redis token 校验 |
| RequestLogAspect | around() | AOP 环绕通知, @Around |
| RateLimitAspect | around() | 自定义注解, AOP |
| UserContext | getUserId(), setUserId() | ThreadLocal |
| AsyncConfig | asyncExecutor() | ThreadPoolTaskExecutor 配置 |

### RAG 能力（全部留 TODO）
| 类 | 核心方法 | 涉及知识点 |
|----|----------|-----------|
| DocumentParserService | parse() | PDFBox, flexmark |
| TextSplitter | split() | chunkSize, overlap |
| EmbeddingService | embed(), embedBatch() | HTTP 调用 bge-m3 |
| PgVectorRetriever | retrieve() | pgvector cosine distance SQL |
| PromptBuilder | build() | System prompt, context 拼接 |
| LlmService | chat() | DeepSeek API 调用 |
| RagPipelineService | ask() | RAG 全流程编排 |
| RelevanceCheckService | isRelevant() | 相似度阈值判断 |
| SourceAssembler | assemble() | 引用信息提取 |
| DocumentIngestService | ingest() | @Async 文档入库编排 |

**要求：** 这些类中的核心方法，Claude Code 不要直接实现完整逻辑，要留 TODO，并在注释里提供范例代码。

---

## 12. 暂不实现内容（第一版）

- ❌ Python LangChain / LlamaIndex
- ❌ LangGraph 编排
- ❌ 微服务架构（Spring Cloud）
- ❌ 消息队列（RabbitMQ / Kafka / Redis Stream）
- ❌ Spring Security（用 Interceptor + Redis 替代）
- ❌ Elasticsearch 全文检索
- ❌ 完整多路召回（BM25 + 向量）
- ❌ reranker（bge-reranker / Cohere）
- ❌ OCR 图片文字识别
- ❌ Kubernetes 容器编排
- ❌ CI/CD 流水线
- ❌ 监控告警（Prometheus / Grafana）

---

## 13. 后续扩展路线

| 阶段 | 扩展项 | 说明 | 预计时间 |
|------|--------|------|----------|
| 第 1 轮扩展 | Redisson 替代手写锁 | 看门狗自动续期、可重入锁 | 第 7 周 |
| 第 1 轮扩展 | 布隆过滤器 | 缓存穿透终极方案 | 第 7 周 |
| 第 1 轮扩展 | 逻辑过期 | 热点 key 防击穿 | 第 7 周 |
| 第 2 轮扩展 | Redis Stream | 文档解析任务队列 | 第 8 周 |
| 第 2 轮扩展 | PostgreSQL 全文检索 | 混合检索（向量 + 关键词） | 第 8 周 |
| 第 3 轮扩展 | 多路召回 | 向量 + BM25 + 关键词三路召回 | 第 9 周 |
| 第 3 轮扩展 | bge-reranker | 对多路召回结果重排序 | 第 9 周 |
| 第 4 轮扩展 | LangGraph 服务化 | RAG 流程编排可视化 | 第 10+ 周 |
| 第 4 轮扩展 | Agent tool calling | LLM 自主决策调用工具 | 第 10+ 周 |
| 持续 | Spring AI 深度集成 | 统一模型调用抽象 | 按需 |

---

> **下一步：** 确认 结构.md 后，后续按 plan.md 的 Day 1 开始生成代码。
