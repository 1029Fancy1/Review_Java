# 面试话术 — KnowledgeHub AI 项目

> 准备日期：2026-06-23 | 面试时间：明天 11:00

---

## 一、自我介绍 + 项目介绍（1.5-2 分钟）

面试官好，我叫蒋舜，目前上海第二工业大学电子信息专业研一在读。

我最近在做的是一个基于 Spring Boot + Redis + PostgreSQL + pgvector + RAG 的个人知识库智能问答系统，叫 KnowledgeHub AI。用户可以上传 PDF 或 Markdown 文档到知识库里，系统解析文档、生成向量存入 pgvector，然后用户用自然语言提问，系统检索相关片段、调用 DeepSeek 生成带引用来源的答案。

我重点参与了后端架构搭建和 Redis 工程实践。后端基于 Spring Boot 三层架构，做了统一响应体、全局异常拦截、AOP 日志、JWT + Redis 混合鉴权。Redis 是我投入最多的——从登录 token、验证码、Cache Aside 缓存开始，逐步覆盖到空值缓存防穿透、随机 TTL 防雪崩、SET NX EX + Lua 分布式锁、Lua 脚本限流、ZSet 热门文档排行榜，一共 13 个 Redis 实战场景。每个场景的代码我自己手敲的，背后的原理和坑也都梳理过。

目前项目还在迭代中，接下来要接入 RAG 问答链路——文档解析、chunk 切分、embedding 生成、pgvector 检索、DeepSeek 调用。之前在另一个项目里我用 Python 做过这条 RAG 核心链路，技术原理是通的，主要是想用 Java 生态重新实现一遍。

---

## 二、Redis 场景速查（面试官问"你用了哪些场景"时用）

### 登录与安全（3 个）
| 场景 | 怎么做的 | 一句话 |
|------|---------|--------|
| JWT 登录 token | JWT 签发后存 Redis，Interceptor 校验，支持踢人下线 | "用 Redis 有状态弥补 JWT 无状态的缺陷" |
| 邮箱验证码 | 随机 6 位数字，存 Redis 5 分钟，校验后删除 | "验证码生命周期管理" |
| 防重复发送 | cooldown key 60 秒，发送前先查是否存在 | "防用户 1 秒内狂点发送" |

### 缓存（3 个）
| 场景 | 怎么做的 | 一句话 |
|------|---------|--------|
| Cache Aside 知识库列表 | 读：缓存 → DB → 回写；写：更新 DB → 删缓存 | "最经典的缓存模式" |
| 文档详情空值缓存 | 不存在的数据存 `"__NULL__"`，TTL 5 分钟 | "防穿透，每次穿透都是无意义的 DB 查询" |
| 随机 TTL | 每个 key 的 TTL 在 30~60 分钟内随机 | "防雪崩，避免批量 key 同时过期" |

### 并发控制（2 个）
| 场景 | 怎么做的 | 一句话 |
|------|---------|--------|
| 分布式锁 | SET NX EX + UUID + Lua 释放 | "防死锁 + 防误删 + 原子释放" |
| 固定窗口限流 | INCR + EXPIRE + Lua 原子执行 | "防刷，保护 LLM 调用成本" |

### 数据场景（4 个，刚完成）
| 场景 | 怎么做的 | 一句话 |
|------|---------|--------|
| 热门文档排行 | ZINCRBY 热度 +1，ZREVRANGE 取 Top N | "文档热度天然就是排行榜" |
| 最近访问知识库 | ZADD time 为 score，ZREVRANGE 取最近 N | "时间戳做 score，自动排序" |
| Hash 解析进度 | HSET 存 status/totalChunks/completedChunks | "前端轮询展示进度条" (待做) |
| 每日额度/幂等 | INCR + 到期清零 / token 一次消费 | "保护 API + 防重复提交" (待做) |

---

## 三、高频追问应答

### Q：空值缓存怎么防穿透？

查 DB 发现数据不存在，不是直接返回 null，而是在 Redis 写 `"__NULL__"`，TTL 5 分钟。下次命中这个标记直接返回"不存在"，不查 DB。为什么 5 分钟？MySQL 自增 ID 不会重用，不存在的 ID 不会突然存在，5 分钟足够防攻击。

### Q：分布式锁的三个关键设计？

1. **SET NX EX 原子加锁** — NX 互斥 + EX 防死锁，必须同一条命令
2. **UUID 做 value** — 释放时校验身份，防止误删别人的锁
3. **Lua 脚本释放** — GET 比对 UUID + DEL 两步原子执行，中间不会有命令插入

### Q：为什么先更新 DB 再删缓存？

反过来（先删缓存再更新 DB）有并发窗口：A 删缓存 → B 读 miss → B 读 DB 旧数据 → B 写缓存旧数据 → A 更新 DB 新数据 → 缓存脏了。先更新 DB 再删缓存，最坏是读到一次旧数据，下次就重建了，最终一致。

### Q：固定窗口限流的边界突刺？

用户可以在窗口切换瞬间发出 2 倍请求（12:00:59 满 20 + 12:01:00 又满 20）。滑动窗口用 ZSet 时间戳可以精准解决，但计算和内存成本更高。知道这个 tradeoff 就行。

### Q：StringRedisTemplate 为什么不用 RedisTemplate？

RedisTemplate 默认 JDK 序列化，存到 Redis 是二进制乱码。StringRedisTemplate 存的是可读 JSON 字符串，运维排查问题可以直接 `GET key`。

### Q：RAG 部分做完了吗？

还没做到，但之前 ServicePilot 项目里我做完了文档解析→切分→embedding→向量入库→检索→LLM生成这条 RAG 核心链路，技术原理是熟的。现在想用 Java 生态重新实现，加深对 Spring Boot 的理解。

---

## 四、引导策略

| 面试官问了... | 往这个话题引 |
|-------------|------------|
| "用了哪些 Redis？" | 13 个场景分类讲，挑锁和限流展开 |
| "遇到过什么难点？" | 分布式锁 UUID 误删 → 引出 Lua 原子释放设计 |
| "为什么这么设计？" | 手写缓存 vs `@Cacheable` 注解，要可控性 |
| "了解集群吗？" | 诚实说项目没涉及，但知道 Lua 的 KEYS/ARGV 分离是为集群兼容 |

---

## 五、面试官预判问题清单（按你的实际进度）

> 带 ⚠️ 的是你项目还没做到但简历提到了的，需要想好怎么接。

---

### 🟢 Spring Boot 基础架构（你全都做过，放心答）

**Q1：三层架构各层职责？**

Controller 接请求、调 Service、返回结果，不写业务逻辑。Service 编排业务逻辑、事务管理、权限校验。Mapper 只做数据库操作。另外 common 层放统一返回体和错误码，exception 层做全局异常拦截。

**Q2：统一返回体 Result 怎么设计的？**

`Result<T>` 三个字段：code（状态码）、message（提示信息）、data（泛型数据）。好处是前端只需要判断 `code === 200`，不用兼容多种返回格式。静态工厂方法 `Result.ok(data)` 和 `Result.fail(errorCode)` 比 new 构造器语义更清晰。

**Q3：全局异常处理怎么做的？**

`@RestControllerAdvice` + `@ExceptionHandler`。业务代码里任何地方抛 `BusinessException`，全局异常处理器自动拦截，转成 `Result.fail()` 返回。避免了 Controller 里到处 try-catch。

**Q4：AOP 日志怎么实现的？**

用 `@Aspect` + `@Around` + `ProceedingJoinPoint`。环绕通知在方法执行前后各插一段逻辑：记录请求 URL、参数、执行耗时。`joinPoint.proceed()` 是原方法执行点，必须调用且必须返回它的结果，否则链就断了。因为 Controller 没实现接口，Spring 默认用 CGLIB 生成子类代理。

**Q5：ThreadLocal 为什么用？不 remove 会怎样？**

ThreadLocal 每个线程私有的储物柜，用来在 Controller → Service → Mapper 整条链路里传递 userId，不用每个方法签名都加参数。Tomcat 用线程池，请求结束线程不销毁，不 remove 的话下个请求复用同一条线程会读到脏数据。在 Interceptor 的 `afterCompletion()` 里 remove，保证无论正常还是异常都会执行。

**Q6：Interceptor 和 Filter 的区别？**

Filter 是 Servlet 容器层面的，优先级更高，可以拦截所有请求包括静态资源。Interceptor 是 Spring MVC 层面的，能拿到 Spring 容器里的 Bean，能做更细粒度的控制。我的 LoginInterceptor 是在 preHandle 里从 Header 拿 token → 查 Redis → 把 userId 放进 ThreadLocal。

**Q7：JWT + Redis 混合鉴权怎么配合的？为什么不用纯 JWT？**

登录成功后生成 JWT token，Redis 里存 `login:token:{token} → userId`，TTL 7 天。Interceptor 从 Header 取 token，查 Redis 是否存在且未过期。纯 JWT 无状态的缺点是服务端无法主动踢人下线——只要 JWT 没过期就能用。加了一层 Redis 后有状态了：删掉 Redis 里的 token，用户的 JWT 虽然还没过期但也通不过校验了。

**Q8：BCrypt 加密原理？**

BCrypt 是单向哈希算法，同一个密码每次加密结果不同（因为随机盐），不可逆。`BCrypt.checkpw(明文, 密文)` 做比对。比 MD5/SHA 更安全，因为可以设 work factor 增加暴力破解成本。

**Q9：MyBatis-Plus 分页插件原理？**

本质是 MyBatis 拦截器。配置 `MybatisPlusInterceptor` + `PaginationInnerInterceptor`，指定 `DbType.POSTGRE_SQL`。调用 `baseMapper.selectPage(page, wrapper)` 时，拦截器先包装 SQL 执行 COUNT 得 total，再根据数据库方言拼 LIMIT/OFFSET，最后组装成 Page 对象。比手写两条 SQL 简洁，换数据库只改 DbType。

**Q10：怎么保证用户只能操作自己的数据？**

两层保障。列表查询用 `wrapper.eq(userId)` 在 SQL 层就过滤了，不会把别人的数据查出来。单条操作用 `getWithCheck()` 方法——查出来先判是否存在，再判 `userId` 是否匹配，不匹配抛异常。update、delete、getDetail 都先调这个方法。

---

### 🟡 Redis 深度追问（你重点投入的，要答出彩）

**Q11：缓存穿透、击穿、雪崩三兄弟？**

穿透 = 查不存在的数据 → 空值缓存 `__NULL__`。击穿 = 热点过期 + 并发 → 分布式锁。雪崩 = 批量同时过期 → 随机 TTL。能各举一个场景说明，面试官就知道你实战过。

**Q12：分布式锁如果锁过期了但业务还没执行完怎么办？**

这就是手写锁的局限。我的方案是 TTL 设业务预估时间的 2-3 倍（文档解析设 10 分钟）。生产级用 Redisson 的看门狗机制——加锁时设默认 30s，后台定时任务每 10s 检查，锁还在且线程还在就自动续期到 30s。知道这个方向就行。

**Q13：限流有哪几种算法？各自优缺点？**

固定窗口（我用的）：简单但有边界突刺。滑动窗口：精确但内存大。令牌桶：允许突发，最实用。漏桶：绝对平滑，但不灵活。我这个项目固定窗口够用，知道其他方案的 tradeoff。

**Q14：Redis 五种数据结构各自适用什么场景？**

String：缓存、token、验证码、分布式锁、限流计数器。Hash：对象属性存储、解析进度。List：消息队列、时间线。Set：标签、共同好友。ZSet：排行榜、按时间排序的最近访问。

**Q15：Redis 过期策略？**

惰性删除：访问 key 时检查是否过期，过期就删。定期删除：每隔 100ms 随机抽一批 key 检查，过期就删。两者结合。Redis 不保证 key 一定在过期瞬间被删，可能稍晚一些。

**Q16：Redis 持久化 RDB vs AOF？**

RDB：定时快照，恢复快但可能丢数据。AOF：记录每条写命令，数据更安全但文件大恢复慢。实际生产一般混合使用。我这个开发项目没开持久化。

**Q17：缓存和数据库双写一致性，除了 Cache Aside 还有哪些方案？**

Read/Write Through：缓存层代理 DB，应用只和缓存打交道。Write Behind：先写缓存异步刷 DB，性能最高但可能丢数据。还有延迟双删——更新 DB → 删缓存 → 等几百毫秒再删一次，把并发窗口的脏数据清掉。

**Q18：为什么手写缓存，不用 Spring Cache 的 @Cacheable 注解？**

`@Cacheable` 不支持空值缓存防穿透，不支持随机 TTL 防雪崩，key 散落在注解字符串里不好管理，出问题时黑盒不好排查。手写每一步都清楚。

---

### 🟠 Spring Boot + Docker 工程类

**Q19：Docker Compose 怎么编排的？**

项目里 `docker-compose.yml` 定义了 PostgreSQL 16（含 pgvector 扩展）和 Redis 7 两个服务，配置了端口映射、数据卷挂载、环境变量。`docker compose up -d` 一键启动。数据库初始化 SQL 用 `docker exec` 导入。

**Q20：项目怎么管理配置文件的？**

`application.yml` 存通用配置（JWT 密钥等），`application-dev.yml` 存开发环境数据源配置。敏感信息如 API Key 从环境变量读取，不硬编码。提供 `.example` 模板文件给其他开发者参考。

**Q21：为什么要用 pgvector 而不是专门的向量数据库？**

数据量不大的情况下，pgvector 让业务数据和向量数据同库存储，少维护一个服务，运维成本低。从简历里 ServicePilot 项目用过 ChromaDB 来看，我是比较过的。大规模场景下 Milvus 这类专用向量数据库检索性能更好。

**Q22：MyBatis-Plus 和 MyBatis 区别？**

MyBatis-Plus 封装了 MyBatis，继承 `BaseMapper` 就自带单表 CRUD，不用写 XML。LambdaQueryWrapper 用 Lambda 表达式构建条件，字段名改了编译期就能发现。分页插件、逻辑删除、自动填充都是开箱即用。

---

### 🔴 ⚠️ RAG 相关（项目还没做到，但简历提到了）

**Q23：RAG 的整体流程是怎样的？**

> 讲你**亲自做过**的部分，不要编没做过的。

我完整做过从文档到问答这一整条链路。具体来说就是文档解析提取文本 → 按 chunkSize 切分 + overlap 保留上下文 → 调 bge-m3 生成 embedding → 向量写入向量数据库 → 用户提问时同样生成 query embedding → 向量检索 Top K → 拼接 prompt → 调 LLM 生成答案。在 ServicePilot 项目里我是用 Python 做的这一套，在 KnowledgeHub 里打算用 Java 重新实现。

**Q24：chunk size 和 overlap 怎么选？**

chunk size 太小语义不完整，太大检索精度下降，通常在 512-1024 token。overlap 保留 10-20%，避免关键信息刚好被切在两段之间。具体参数要根据实际效果调。这个我自己在写 TextSplitter 的时候实际调过。

**Q25：embedding 模型为什么选 bge-m3？**

BGE-M3 是 BAAI 开源的，支持中英文、1024 维、支持稠密检索，开源免费可以本地部署。我实际调用过 bge-m3 的 API 生成向量。

**Q26：向量数据库怎么选的？**

KnowledgeHub 用 pgvector，因为业务数据和向量数据同库存储，少维护一个服务。ServicePilot 用的 ChromaDB，轻量适合 Python 生态。两者都实际用过，选择取决于技术栈和数据规模。

**Q27：拒答机制怎么做？**

我理解的方案是：检索后判断 Top K 最高相似度是否低于阈值（比如 0.5），低于就说明知识库里没有相关内容，返回"抱歉，知识库中没有相关信息"。这块我设计了但还没写代码。

**Q28：RAG 和 Fine-tuning 的区别？**

RAG 不改变模型参数，知识是外挂的，更新知识库就更新答案，可解释性强（有引用来源）。Fine-tuning 把知识融入模型参数，推理更快但更新成本高，无法溯源。

---

### 🔴 ⚠️ ServicePilot 项目（简历写了很多，实际只做了 RAG 链路）

> 面试官看到简历上 ServicePilot 列了 LangChain、Agent、Tool Calling、Benchmark，大概率会追问。
> 核心策略：**诚实说哪些是自己做的，哪些还在学。**

**Q29：ServicePilot 你做完了吗？**

还没有完全做完。我目前自己做完了文档解析 → 文本切分 → embedding 向量化 → 向量入库 → 提问检索 → 答案生成这一条 RAG 核心链路，前后端能跑通。LangChain 的编排、Agent 的 Tool Calling、Benchmark 自动化评测这些，方案我调研过了但代码还没写，是我接下来要推进的方向。

**Q30：那简历上为什么写了 LangChain、Agent、Tool Calling？**

这些是我设计好但还没完全实现的模块。比如 Agent 这块，我已经设计好了 5 个工具（知识库检索、订单查询、工单创建/查询、转人工），Tool Calling 的机制——模型返回 function_call → 解析参数 → 调用对应函数 → 把结果再喂给模型——这个流程我是清楚的，但具体代码在逐步实现中。LangChain 我了解它的 Chain、PromptTemplate、Memory 这几个核心模块的用法，只是还没有全部在项目里落地。

**Q31：那你 ServicePilot 里到底做了哪些？能讲具体一点吗？**

> 这是最关键的问题，讲你真实做过的。

我做的那部分是：用 FastAPI 搭后端，用户上传 PDF → PyPDF2 解析提取文本 → TextSplitter 按 512 token 切分带 overlap → 调 BGE Embedding API 生成向量 → 存入 ChromaDB → 用户 POST 问题 → 同样的 embedding 向量化 → ChromaDB 做 Top K 检索 → 拼接 system prompt + 检索到的上下文 + 用户问题 → 调 DeepSeek API 生成答案 → 返回给前端。Chat 部分支持流式输出，前端用 SSE 接收。这条链路从头到尾是我自己写的。

**Q32：Benchmark 评测你懂吗？**

方案我了解但没实际做。核心思路是准备一个测试集（问题 + 期望答案），用自动化脚本逐条提问，评估指标包括检索的 Recall@K（Top K 里有没有正确答案）、答案的准确性（用 LLM 打分或人工评估）。自动化评测脚本写了一半还没来得及集成到项目里。

---

### 🔵 行为面试类

**Q33：你做这个项目最大的收获是什么？**

最大的收获是 Redis 从"背八股"变成"能写能用"。以前我只是知道缓存穿透、分布式锁这些概念，但这个项目我从 SET NX EX 到 Lua 脚本全手敲了一遍，懂了为什么这么做、每个设计的坑在哪、面试官追问怎么接。另外 Spring Boot 的分层架构、AOP、Interceptor 这些也不再是抽象的面试题，实际写过之后理解深了很多。

**Q34：开发中遇到过什么问题？**

> 可以从之前的开发经历里挑一个真实的讲。

比如最开始写分布式锁的时候，我只做了 SET NX EX，没想释放锁的问题。后来意识到如果锁过期了、被别人拿到了、我再释放就会误删别人的锁。这就是为什么需要 UUID 做 value 校验 + Lua 脚本原子释放。写代码之前觉得分布式锁很简单一条命令，写完才知道里面的门道。

**Q35：你在项目里扮演什么角色？**

> 如果是个人项目，诚实说。

这是我个人学习项目，后端架构、代码全部是我自己搭建和写的。Claude Code 帮我生成基础骨架和配置，核心业务逻辑——尤其是 Redis 的 13 个场景——全部我自己手敲，每个 TODO 我都是理解了原理再写的。

---

## 六、面试前快速过一遍

1. ✅ 自我介绍 1.5 分钟
2. ✅ Redis 四类场景随口说出
3. ✅ 分布式锁三要素：NX+EX 原子 / UUID / Lua 原子释放
4. ✅ 缓存三兄弟：穿透→空值缓存、击穿→分布式锁、雪崩→随机 TTL
5. ✅ Cache Aside：读路径 + 写路径 + 为什么先更新 DB
6. ✅ 限流：Lua 脚本 + count==1 才 EXPIRE + 边界突刺

---

> Good luck 🍀 面完回来说一声。
